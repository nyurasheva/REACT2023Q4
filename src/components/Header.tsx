import ErrorThrowButton from './ErrorThrowButton';

const Header = () => {
  return (
    <header className="header container">
      <div className="row">
        <ErrorThrowButton />
      </div>
    </header>
  );
};

export { Header };
